# FlightBuddy Codetations Demo Application

To set up client:

1. `cd client`
2. `npm install`
3. `npm start`

To set up server:

1. `cd server`
2. `pip install -r requirements.txt`
3. `python3 app.py`
